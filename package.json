{
  "name": "cybervett",
  "version": "3.0.0",
  "private": true,
  "description": "Responsible, structured technical interviews with human-controlled hiring decisions.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --workspace @cybervett/contracts && npm run build --workspace @cybervett/api && npm run build --workspace @cybervett/web",
    "typecheck": "npm run build --workspace @cybervett/contracts && npm run typecheck --workspace @cybervett/contracts && npm run typecheck --workspace @cybervett/api && npm run typecheck --workspace @cybervett/web",
    "test": "npm run test --workspaces --if-present",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
